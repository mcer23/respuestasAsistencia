api {
    baseUrl = "http://localhost:8080",
    path = "/api/v1",
    endpoints = {
        facturas = "/facturas"
    }
    headers = {
        contentType = "application/json",
        accept = "application/json"
    }
}